# Synthetic-only local development defaults. NOT production values.
# No production credentials, customer data, cloud endpoints, or secrets.
POSTGRES_DB=oshe_dev
POSTGRES_USER=oshe_dev
POSTGRES_PASSWORD=oshe_dev_synthetic_only
MEILI_MASTER_KEY=oshe_dev_synthetic_only
