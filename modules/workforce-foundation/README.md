# Workforce Foundation

`MOD-WRF` is a synthetic, local in-memory foundation for V050-E005. A person
holds one immutable opaque `usr_` trusted-identity reference without copying
authentication truth. Employment and ownership are explicit tenant-scoped
relationships with append-only attribution; they grant no membership, role,
authorization, database persistence, migration, external identity sync, or
operational authority. All identifiers and fixtures are synthetic.
