# Tests

Architecture, unit, property, integration, contract, permission, isolation, security, offline, recovery, and release qualification tests.

- `architecture/` verifies module and dependency boundaries.
- `integration/` verifies supported cross-component behavior.
- `security/` holds authorization, isolation, privacy, and supply-chain verification.
- `release/` holds release-gate and artifact-evidence verification.
